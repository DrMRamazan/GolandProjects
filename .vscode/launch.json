{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Терминал отладки JavaScript",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}